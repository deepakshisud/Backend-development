<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book</title>
</head>
<body>
    <h1>Edit a book</h1>
    <form action="/books/<%= foundBook._id %>?_method=PUT" method="POST">
        <label for="name">Book name</label>
        <input type="text" name="name" placeholder="Book name" id="name" value="<%= foundBook.name %> ">
        <label for="price">Book price</label>
        <input type="number" name="price" placeholder="Price" id="price" value="<%= foundBook.price %>">
        <label for="category">Select Category</label>
        <select name="category" id="category">
            <option value="romance" <%= foundBook.category === 'romance' ? 'selected': ''%> >romance</option>
            <option value="horror" <%= foundBook.category === 'horror' ? 'selected': ''%>>horror</option>
            <option value="thriller" <%= foundBook.category === 'thriller' ? 'selected': ''%>>thriller</option>
            <option value="comedy" <%= foundBook.category === 'comedy' ? 'selected': ''%>>comedy</option>
        </select>
        <button>Add book</button>
    </form>

    <a href="/books/<%= foundBook._id %> ">Cancel</a>
    
</body>
</html>